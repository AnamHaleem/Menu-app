# Frontend environment variables
# For local dev this can be your local API
# For production, set this to your Railway backend domain (with or without /api)
VITE_API_URL=http://localhost:3001

# Optional: if omitted, app runs in Guest Mode without Clerk auth
VITE_CLERK_PUBLISHABLE_KEY=
